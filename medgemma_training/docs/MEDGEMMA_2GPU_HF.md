# Historical MedGemma guide

This filename is historical, not a two-GPU restriction. See the [authoritative repository workflow](../../README.md#4-medgemma-training), which supports configurable positive GPU counts on one machine.

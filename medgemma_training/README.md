# MedGemma training

The complete configurable-GPU smoke, training, and recovery workflow is in the [repository README](../README.md#4-medgemma-training).

Use `run_medgemma.py` from the repository checkout. Preserve exact saved sources and the original world size/accumulation for recovery; historical archives and completed results must not be rewritten.

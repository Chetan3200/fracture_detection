# Install with the SAME python that passed your CUDA test.
# This exact torch requirement preserves your working installed build.
# Do not add --upgrade, replace CUDA, or install FlashAttention for this script.
torch==2.11.0+cu128
transformers==4.57.6
peft==0.18.1
accelerate==1.12.0
bitsandbytes==0.49.2
huggingface-hub==0.36.2
tokenizers==0.22.2
safetensors==0.7.0
# Pillow already exists in your image-preparation environment.
Pillow>=10.0
